import { StubPage } from '../components/StubPage';

export function AnalyticsPage() {
  return <StubPage title="Analytics" hint="Global scan and click analytics with per-QR and per-link breakdowns." />;
}
