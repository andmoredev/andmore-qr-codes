import { StubPage } from '../components/StubPage';

export function PageEditorPage() {
  return <StubPage title="Links Page Editor" hint="Edit links (drag to reorder), profile info, theme, and publish status." />;
}
