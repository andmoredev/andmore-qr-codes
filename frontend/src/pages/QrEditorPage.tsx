import { StubPage } from '../components/StubPage';

export function QrEditorPage() {
  return <StubPage title="QR Editor" hint="Create or edit a QR code: direct URL or Links-Page-backed, with optional logo." />;
}
